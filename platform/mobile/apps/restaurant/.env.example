# =============================================================================
# Noowe Restaurante — variáveis de ambiente (desenvolvimento local)
#
# Uso:
#   cp .env.example .env
#
# No Expo, variáveis expostas ao app DEVEM começar com EXPO_PUBLIC_
# Após alterar o .env, reinicie o Metro ou rode de novo: npx expo run:ios
# =============================================================================

# --- Obrigatórias (Supabase) ---
# Dashboard Supabase > Project Settings > API
# Usadas por shared/config/env.ts e shared/services/supabase.ts
EXPO_PUBLIC_SUPABASE_URL=https://SEU_PROJETO.supabase.co
EXPO_PUBLIC_SUPABASE_PUBLISHABLE_KEY=sb_publishable_xxxxxxxx
# Alternativa legada (use publishable key se disponível):
EXPO_PUBLIC_SUPABASE_ANON_KEY=eyJhbGciOiJIUzI1NiIsInR5cCI6IkpXVCJ9...
# Nunca use SUPABASE_SERVICE_ROLE_KEY em apps Expo/mobile.

# --- Google OAuth (opcional) ---
# Google Cloud Console > APIs & Services > Credentials
# Também habilite Google em Supabase Dashboard > Authentication > Providers.
# Sem flag + client IDs, o botão de Google fica oculto/desabilitado.
EXPO_PUBLIC_SUPABASE_AUTH_GOOGLE_ENABLED=false
EXPO_PUBLIC_GOOGLE_CLIENT_ID=xxxxx.apps.googleusercontent.com
EXPO_PUBLIC_GOOGLE_IOS_CLIENT_ID=xxxxx.apps.googleusercontent.com
EXPO_PUBLIC_GOOGLE_ANDROID_CLIENT_ID=xxxxx.apps.googleusercontent.com

# --- Apple OAuth / Biometria (opcional) ---
# Habilite Apple em Supabase Dashboard > Authentication > Providers antes de mudar para true.
EXPO_PUBLIC_SUPABASE_AUTH_APPLE_ENABLED=false
# Biometria só desbloqueia sessão Supabase já persistida no dispositivo.
EXPO_PUBLIC_ENABLE_BIOMETRIC_LOGIN=false

# --- Sentry (opcional) ---
# shared/config/sentry.ts — deixe vazio para desabilitar em dev
EXPO_PUBLIC_SENTRY_DSN=
EXPO_PUBLIC_ENV=development

# Overlay de debug na tela de boot (app/index.tsx)
EXPO_PUBLIC_LAUNCH_DEBUG=true

# =============================================================================
# Notas
# -----------------------------------------------------------------------------
# API / WebSocket: em dev, shared/config/env.ts usa http://localhost:3000 fixo.
# Não é necessário definir API_URL ou WEBSOCKET_URL neste .env.
#
# Builds de produção (EAS): variáveis como API_BASE_URL, WS_URL, SENTRY_DSN e
# FIREBASE_* são definidas em eas.json ou via `eas secret:create`, não no .env local.
#
# E-mail de cadastro (Resend): NÃO configure RESEND_* neste arquivo.
# A Edge Function register-with-resend lê os secrets no Supabase:
#   cd platform/supabase
#   cp .env.example .env   # preencha RESEND_API_KEY e RESEND_FROM_EMAIL
#   supabase secrets set --env-file .env --project-ref SEU_PROJECT_REF
# =============================================================================
